# Filtered-path probe

This file lives under `dist/`, which the canonical `.coderabbit.yaml`
`path_filters` exclude (`!**/dist/**`). CodeRabbit should not select it for
review. If this text were reviewed, the filter would be broken.
