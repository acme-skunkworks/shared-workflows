name: Changelog enrich

# Post-merge dated-changelog enrich for this deploy target (A-800 / A-821).
# Never pull_request_target. App-token write-back requires secrets: inherit.
# Pin is the A-821 merge SHA until a release that includes it moves @v1.
on:
  push:
    branches:
      - main

concurrency:
  group: changelog-enrich-${{ github.ref }}
  cancel-in-progress: false

permissions: {}

jobs:
  changelog-enrich:
    uses: acme-skunkworks/shared-workflows/.github/workflows/reusable-changelog-enrich.yml@80681b730868ffd99b78c7b1385d20c3c2f7f8dc
    with:
      mode: enrich
    permissions:
      contents: read
      pull-requests: read
    secrets: inherit
