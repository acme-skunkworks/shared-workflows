# A-674 live verification (throwaway)

Temporary marker on a non-workflow path, so claude-code-action's workflow-validation
guard passes (the review workflow file matches `main`) and the review runs fully —
reaching the internal `git fetch` that A-674 fixes. Confirms the review now gets
past the fetch and posts. This PR will be closed unmerged.
