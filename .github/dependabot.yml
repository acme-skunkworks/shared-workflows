version: 2

updates:
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
    # Group all action bumps into a single PR per week. Keeps notification
    # noise low and ensures SHA-pinned `uses:` lines stay current across every
    # workflow. Grouped PRs land or revert atomically. This ecosystem also
    # covers reusable-workflow `uses:` refs, so the dogfood callers' local refs
    # and (in consumers) cross-repo `@<sha>` refs are kept fresh the same way.
    groups:
      actions:
        patterns: ["*"]
    commit-message:
      prefix: "ci"
      include: scope

  - package-ecosystem: npm
    directory: "/"
    schedule:
      interval: weekly
    # Dev-only toolchain (markdownlint, prettier). Grouped + low-noise.
    groups:
      dev-dependencies:
        patterns: ["*"]
    # `prefix-development` is set explicitly alongside `prefix`: this ecosystem
    # is dev-only, and `prefix` on its own leaves development-dependency bumps
    # on Dependabot's default subject rather than the Conventional `chore(…)`
    # the commit gate expects (A-980 / A-823).
    commit-message:
      prefix: "chore"
      prefix-development: "chore"
      include: scope
